# Database — Local PostgreSQL (default)
DATABASE_URL=postgresql://qualyx:password@localhost:5432/qualyx

# Database — Supabase (uncomment these, comment out the local DATABASE_URL above)
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# DATABASE_URL_DIRECT=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# DATABASE_SSL=true

# NextAuth
AUTH_SECRET=generate-with-openssl-rand-base64-32
AUTH_URL=http://localhost:3000

# Mailosaur (Phase 3)
# MAILOSAUR_API_KEY=
# MAILOSAUR_SERVER_ID=
